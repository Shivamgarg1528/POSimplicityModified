<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_setting_drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:openDrawer="start">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include layout="@layout/common_toolbar" />

        <FrameLayout
            android:id="@+id/activity_setting_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </LinearLayout>

    <android.support.design.widget.NavigationView
        android:id="@+id/activity_setting_navigation"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:scrollbars="none"
        app:itemBackground="@color/colorPrimaryDark"
        app:itemIconTint="@drawable/selector_navigation_item"
        app:itemTextColor="@drawable/selector_navigation_item"
        app:menu="@menu/activity_setting_menu"
        app:theme="@style/NavigationDrawerStyle" />

</android.support.v4.widget.DrawerLayout>
